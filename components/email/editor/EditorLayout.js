// /components/email/editor/EditorLayout.js
// FULL REPLACEMENT — centered canvas + correct drag insertion


// Placeholder EditorLayout until new email editor is built
import { useEffect } from "react";

export default function EditorLayout() {
  // Placeholder UI
  useEffect(() => {
    // Any setup if needed
  }, []);
  return (
    <div style={{ padding: 40, textAlign: "center", color: "#888" }}>
      <h2>Email Editor Coming Soon</h2>
      <p>The new email editor will be available here soon.</p>
    </div>
  );
}
